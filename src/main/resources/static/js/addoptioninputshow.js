$('#option100input').on('keyup', function () {
    var $this = $(this),
        val = $this.val();
    if (val.length >= 1) {
        $('#option200text').show(100);
    } else {
        $('#option200text').hide(100);
    }
});

$('#option200input').on('keyup', function () {
    var $this = $(this),
        val = $this.val();
    if (val.length >= 1) {
        $('#option300text').show(100);
    } else {
        $('#option300text').hide(100);
    }
});

$('#option300input').on('keyup', function () {
    var $this = $(this),
        val = $this.val();
    if (val.length >= 1) {
        $('#option400text').show(100);
    } else {
        $('#option400text').hide(100);
    }
});

$('#option400input').on('keyup', function () {
    var $this = $(this),
        val = $this.val();
    if (val.length >= 1) {
        $('#option500text').show(100);
    } else {
        $('#option500text').hide(100);
    }
});

$('#option500input').on('keyup', function () {
    var $this = $(this),
        val = $this.val();
    if (val.length >= 1) {
        $('#option600text').show(100);
    } else {
        $('#option600text').hide(100);
    }
});

$('#option600input').on('keyup', function () {
    var $this = $(this),
        val = $this.val();
    if (val.length >= 1) {
        $('#option700text').show(100);
    } else {
        $('#option700text').hide(100);
    }
});

$('#option700input').on('keyup', function () {
    var $this = $(this),
        val = $this.val();
    if (val.length >= 1) {
        $('#option800text').show(100);
    } else {
        $('#option800text').hide(100);
    }
});

$('#option800input').on('keyup', function () {
    var $this = $(this),
        val = $this.val();
    if (val.length >= 1) {
        $('#option900text').show(100);
    } else {
        $('#option900text').hide(100);
    }
});